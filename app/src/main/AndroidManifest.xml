<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.little.easymv"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <application

        tools:replace="android:theme"

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name="com.little.easymv.base.App"
        android:theme="@style/AppTheme.NoActionBar">


        <activity android:name="com.little.easymv.FlashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="com.little.easymv.MainActivity"
                  android:theme="@style/AppTheme.NoActionBar.BG"
                  android:launchMode="singleTask">

        </activity>
    </application>

</manifest>